import { colors } from './colors';

export const backgroundColors = {
  backgroundColorOpacity: `${colors.secondColor}99`,
  cardBackgroundColorOpacity: '#FFFFFF90',
};

export default backgroundColors;
